import 'package:flutter/material.dart';

class CommomWordsViewBody extends StatelessWidget {
  const CommomWordsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(10),
      child: Column(
        children: [],
      ),
    );
  }
}
